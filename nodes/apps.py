"""Config for the nodes app."""

from django.apps import AppConfig


class NodesConfig(AppConfig):
    """Config for the nodes app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "nodes"
